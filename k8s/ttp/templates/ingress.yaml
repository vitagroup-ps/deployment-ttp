apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name:  {{ .Values.ttp.name }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  {{- if .Values.ttp.ingress.tls.enabled }}
  tls:
    - hosts:
        -  {{ .Values.ttp.ingress.host }}
      secretName:  {{ .Values.ttp.ingress.tls.secret }}
  {{- end }}
  rules:
    - host:  {{ .Values.ttp.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ttp.name }}
                port:
                  number: {{ .Values.ttp.service.ports.http }}