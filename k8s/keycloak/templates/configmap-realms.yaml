apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.keycloak.name }}-realms
data:
{{- $paths := .Files.Glob "realms/*.json" }}
{{- range $path, $_ := $paths }}
  {{ base $path }}: |
{{ tpl ($.Files.Get $path) $ | indent 4 }}
{{- end }}